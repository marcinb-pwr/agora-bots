{
  "name": "@agora-bots/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "pg": "8.16.3"
  },
  "scripts": {
    "build": "node --check scripts/migrate.mjs",
    "lint": "eslint scripts test",
    "migrate": "node scripts/migrate.mjs",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run test/integration",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "8.15.5",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
